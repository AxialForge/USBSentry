# Python
__pycache__/
*.pyc

# PyInstaller build output
build/
dist/
*.spec
# Note: USBSentry.exe is intentionally committed to the repo (per request).

# Local runtime files (personal to each user's machine)
config.json
history.csv
